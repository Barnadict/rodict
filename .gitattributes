# Normalize line endings to LF in the repo (consistent across Windows dev + Linux CI)
* text=auto eol=lf

# Binary files — never touch
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text eol=lf
