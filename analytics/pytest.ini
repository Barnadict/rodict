[pytest]
# `pythonpath = .` puts analytics/ on sys.path so tests import the job modules
# (deadrule, anomaly, ...) the same way the jobs import each other — the package
# is run as loose scripts by run.py, not installed.
pythonpath = .
testpaths = tests
