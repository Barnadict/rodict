# Python analytics jobs (Phase 4). Precompute heavy stats into the
# AnalyticsResult table; the Next.js frontend only reads the results.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lifelines>=0.27
# Connects the jobs to the hosted Turso DB (Task #33). Local runs against a
# `file:` DATABASE_URL use stdlib sqlite3 and never import this.
libsql>=0.1.11
