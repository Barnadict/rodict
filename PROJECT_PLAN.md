# rodict — Roblox Game Trends & Statistics Website — Project Plan

## ▶️ SESSION START — copy/paste this at the beginning of every new session

```
Read PROJECT_PLAN.md. All 40 tasks are DONE (#12 closed as obsolete — #32's cloud cron replaced it), plus a scale-up (#41). The build is finished and live; the project is deliberately PAUSED to accumulate history. Don't start new work — ask me what I want first.
Follow the plan's rules: optimize as you go, label all estimates/forecasts explicitly,
and remind me before any task that needs a prerequisite I must provide.
```

> ## ✅ Build complete — paused to accumulate data (2026-07-17)
>
> **All 40 tasks are resolved, plus a corpus scale-up (#41).** The site is live and verified in production: collected every 3h by GitHub Actions into Turso, analytics recomputed after each successful run, failures open a GitHub issue, and the footer reports collection freshness. `/about` documents every assumption from the constants the app actually computes with. 161 tests run in CI on every push. Every route is partially prerendered — static shell plus data cached at `cacheLife("hours")` — instead of re-querying the DB per request (confirmed live via `X-Nextjs-Prerender: 1`).
>
> **Corpus scaled ~343 → thousands (#41, 2026-07-17):** discovery was silently capped at one shallow search page (20 results/query); it now paginates omni-search and pulls the explore-api charts, and the collector's write path was rebuilt to bulk operations so a multi-thousand-game run persists in seconds of DB time. A local end-to-end test grew a scratch DB 30 → 4,188 games in ~6 min with the data integrity intact. **This makes the pause more valuable, not less:** ~10× more games are now accumulating the history the lifespan/forecast features are gated on, and history never backfills — so doing this before the wait, rather than after, is worth ~10× the eventual data. The genre/game counts in older notes below (380 games / 18 genres) predate this and will climb over the next few cron runs.
>
> **Why it's paused, not abandoned:** the headline features — genre lifespan (Kaplan-Meier), seasonality, and forecasts — are gated on _weeks_ of accumulated snapshots, and still correctly report `insufficient`. That's the cold-start caveat this plan called out on day one, working as designed. **No code will fix it; only time will.** The collector runs unattended, so those pages improve on their own. Check back in a few weeks.
>
> **When you return, the two ideas already on the table** (neither started, both deliberate): **update/relaunch impact** — correlate spikes with game updates, which needs `robloxUpdatedAt` recorded _per snapshot_ (it currently lives only on `Game`, so update history isn't retained) — and **hardening**: `alert-on-failure.yml` has still never actually fired, and the anomaly `MIN_ABS_CHANGE` threshold (0.25) was tuned against 30 games and deserves a re-look at 380.
>
> **⚠️ Two things only you can do:** (1) after committing/pushing, run the **"Deploy migrations"** workflow from the Actions tab to apply Task #39's `add_current_favorites_index` to Turso. (`npm run db:deploy` **cannot** work from a dev machine — the Turso credentials only exist in GitHub secrets and Vercel, so the script correctly refuses a local `file:` URL. `migrate.yml` was added for exactly this.) (2) **Fix the Vercel project settings** — Deployment Protection is now off, and that revealed the deployment has never actually served the app: every route returns Vercel's platform 404 while `public/` assets return 200, meaning Vercel is publishing this as a static site and never running Next.js. Set **Framework Preset = "Next.js"** and clear any **Output Directory** override in Vercel → Settings → Build & Deployment. See Task #31 for the full diagnosis.
>
> **One-time (first session after the folder was renamed to `rodict`):** also say
> _"restore the memory notes from docs/claude-memory"_ so Claude re-creates the per-project memory at the new path.

---

**Site name:** rodict
**Reference sites:** [romonitorstats.com](https://romonitorstats.com/), [rolimons.com/games](https://www.rolimons.com/games)
**UI style:** [devforum.roblox.com](https://devforum.roblox.com/) — dark + light mode
**Recommended stack:** Next.js + TypeScript · Tailwind + shadcn/ui · Prisma (SQLite locally → Neon/Turso hosted) · Recharts · Node collector + Python analytics jobs on GitHub Actions · **hosting: Vercel**

**Goal:** A website Roblox developers use to spot game genres and trends worth building next — by showing statistics (never telling them what to do). Genre popularity, earnings estimates, concurrent players, how long a genre lasts before dying off, all sortable by date.

**Current scope:** Functional for personal use. **Later (out of scope now):** accounts, verification, ads.

---

## How to resume across sessions

1. Open a new session in this project.
2. Say: **"Read PROJECT_PLAN.md. Last finished task: #N — continue."**
3. Claude reads this file + existing code, then does task **#N+1** and checks it off here.

The plan is fully editable — reorder, rescope, swap the stack, or add/remove tasks anytime.
**One time-sensitive note:** historical trend data only accumulates from the day the collector (Task #8/#12) first runs — don't delay that part. The data-science phase (Phase 4) also depends on having weeks of accumulated snapshots.

---

## Locked decisions

These were decided up front because they shape Phase 1 data design:

- **Collection scope:** track top games **AND** discover new/rising games near launch, then keep following them as they decline. This avoids **survivorship bias** — without it, dead games never enter the dataset and every survival/lifespan stat is wrong.
- **Definition of "dead"/"died off":** a game is considered dead when its concurrent player count stays **below ~5% of the game's all-time peak for 7+ consecutive days**. Tunable, but this is the operational rule survival analysis uses.
- **Genre taxonomy:** maintain our **own normalized genre list** and map Roblox's native tags onto it, with manual overrides for mislabeled games. Genre drives every statistic, so its quality matters most.

---

## Prerequisites you (the user) must provide

Claude cannot create external accounts, verify emails, enter payment info, or click OAuth/login screens. These are on you — but **nothing here is needed to start** (Tasks #1–#29 are fully local). **Reminder protocol: when Claude reaches a task that needs one of these, it will pause and remind you before proceeding.**

Local tooling (already verified present): Node v24 ✅ · npm v11 ✅ · git 2.54 ✅ · Python 3.10 ✅. Optional: `gh` (GitHub CLI) not installed — installing it smooths GitHub setup but isn't required.

| #   | What you provide                                                            | Needed at                      | Cost      | Notes                                                                                     |
| --- | --------------------------------------------------------------------------- | ------------------------------ | --------- | ----------------------------------------------------------------------------------------- |
| 1   | **GitHub account + repo** (push access)                                     | Task #1 (init) / #32 (Actions) | Free      | Public repo = unlimited Actions minutes; private = limited. **Decide public vs private.** |
| 2   | **Vercel account** (hosting)                                                | Task #31                       | Free      | Sign up via GitHub login; authorize it.                                                   |
| 3   | **Hosted database** (Neon or Turso) → give Claude the **connection string** | Task #30                       | Free tier | The single most important credential.                                                     |
| 4   | **GitHub Actions secrets** (paste DB string + tokens into repo settings)    | Task #32                       | Free      | Claude prepares the workflow + tells you exactly what to paste; you enter the values.     |
| 5   | **Backup destination** (optional) — bucket or private repo for DB exports   | Task #11                       | Free      | Only if you want off-site backups of irreplaceable history.                               |

**Settled choices:** site name = **rodict** · hosting = **Vercel** · collection frequency = **default (hourly), tunable later** · custom domain = **skip for now** (free `*.vercel.app` URL).

**Handled by research, not required from you** (✅ confirmed): earnings-estimation assumptions (#6, now in Task #9) and the normalized genre list (#7, now in Task #5).

---

## Standing requirement: performance & optimization

**Optimize aggressively at every step — this is a rule that applies to all work, not a one-time task.** As features, data, and analytics grow, performance must be protected continuously or it degrades. Apply throughout:

- **Data/DB:** index snapshot tables on lookup columns (gameId, genre, collectedAt); paginate and aggregate in SQL, never load full tables into memory; precompute expensive analytics into result tables (never compute on request).
- **Frontend:** server-render/stream where possible, code-split, lazy-load charts, virtualize long tables, memoize, keep bundles lean, optimize images/thumbnails.
- **Caching:** cache Roblox API responses and computed query results; use incremental revalidation for pages reading mostly-static precomputed data.
- **Collector/analytics:** batch API calls, respect rate limits, only recompute what changed.
- **Guardrail:** each new feature must not regress page load or query time — verify before considering a task done.

---

## Key feasibility notes

- **Doable now:** game stats (playing count, visits, favorites, likes/dislikes, active/dead), genre stats, trends over time, sort/filter by date.
- **Trends over time** require _your own_ stored snapshots — Roblox APIs return a snapshot of _now_, not history. The collector builds that history over time.
- **Earnings = ESTIMATES.** Roblox has no public revenue API. Like Rolimons/RoMonitor, earnings are derived from indirect signals. **Every estimated or forecasted value is labeled explicitly** (inline "Est."/"Projection" badges + the "About the data" page). Nothing derived is shown as official Roblox data.
- **Roblox APIs** are rate-limited and semi-official — the plan builds in caching + a polite scheduled collector.
- **Cold start:** the site launches with near-zero history. Trend and analytics features are intentionally sparse for the first few weeks until snapshots accumulate — this is expected, not a bug.

---

## Hosting & automated collection (free-tier architecture)

Static/serverless hosts (Netlify/Vercel) **cannot** run the collector alone — ephemeral filesystem (a SQLite file won't persist) and short function timeouts. The free-tier split that works:

| Piece         | Free option                                                      | Role                                                                      |
| ------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------- |
| **Frontend**  | Vercel (free)                                                    | Serves the website                                                        |
| **Database**  | Neon (Postgres) or Turso (hosted SQLite) — free tier, persistent | Stores snapshots + precomputed analytics                                  |
| **Collector** | GitHub Actions scheduled workflow (cron, free minutes)           | Polls Roblox on a schedule, writes snapshots to the hosted DB             |
| **Analytics** | GitHub Actions scheduled workflow (Python)                       | Precomputes heavy stats (survival, clustering, forecasts), stores results |

**Why this works:** GitHub Actions is a free, reliable cron scheduler. Heavy math is precomputed offline in Python (strong stats libraries) and stored — the frontend only _reads_ results, so nothing heavy runs in the browser or a timed-out function. Local dev uses SQLite; because we use Prisma, moving to the hosted DB is a config change, not a rewrite.

---

## Risks & operational concerns

- **Datacenter IP blocking:** Roblox may rate-limit or block cloud IPs (GitHub Actions runners) more aggressively than home IPs. Mitigation: polite backoff, caching, low frequency; fallback to a cheap alternative runner or self-hosted runner if blocked.
- **ToS / legal:** scraping and redisplaying Roblox data has Terms-of-Service implications even for personal use. Stance: respect rate limits, attribute Roblox as the source, keep it informational/personal, and document this on the "About the data" page.
- **Free-tier DB limits:** snapshots grow forever; the retention/downsampling job (Task #11) keeps size within Neon/Turso free caps.
- **Irreplaceable history:** accumulated snapshots cannot be re-fetched — if the DB is lost, that history is gone permanently. Mitigation: periodic export/backup (Task #11).
- **Collector reliability:** unattended jobs fail silently (endpoint changes, blocks). Monitoring (Task #34) alerts on failures and surfaces "last successful collection."

---

## Definitions

- **Dead / died off:** concurrent players below ~5% of the game's all-time peak for 7+ consecutive days (used by survival analysis).
- **Active:** not dead — has meaningful concurrent players relative to its own history.
- **Genre:** our normalized _gameplay_ genre label (mapped from Roblox tags), not Roblox's deprecated thematic `Genre` enum. See Task #5 for the list.
- **Theme:** a cross-cutting tag (e.g. Anime, Fantasy, Sci-Fi) tracked separately from genre.
- **Estimated earnings:** a derived range from public signals × tunable assumptions, converted via the DevEx rate constant. Always labeled "Est." — never presented as real revenue. See Task #9.
- **All timestamps stored in UTC** — display in local time only in the UI.

---

## Task list

Legend: `[ ]` todo · `[x]` done
Model hint (right after each task number): 🟢 = Sonnet can handle it · 🟡 = borderline (Sonnet can attempt, Opus safer) · 🔴 = use Opus. These are my judgment calls; you switch models manually after each task.

### Phase 0: Foundation

- [x] **1.** Scaffold Next.js + TypeScript project; set up Tailwind, ESLint/Prettier, folder structure. Confirm dev server runs. **Initialize a git repo, add `.gitignore` + `.env.example`, and create a GitHub remote** (required later for GitHub Actions automation in Phase 5). ✅ Done — Next.js 16 + React 19 + Tailwind v4 (App Router, `src/`, `@/*` alias); Prettier wired into ESLint; `.env.example` + `.gitattributes`; local git repo initialized (2 commits). **GitHub remote deferred** — not needed until Task #32; will remind.
- [x] **2.** 🟢 Install & configure shadcn/ui. Build base layout: top nav bar, sidebar, content area, footer — laid out like devforum.roblox.com. ✅ Done — shadcn/ui initialized (`base-nova` style, Base UI primitives, neutral base color); added sidebar, sheet, tooltip, avatar, dropdown-menu, input, scroll-area, badge, skeleton components. Built `AppSidebar` (collapsible, devforum-style nav: Dashboard/Games/Genres/Trending + Watchlist/About), `SiteHeader` (sidebar trigger, search input, spot reserved for Task #3's theme toggle), `SiteFooter` (unofficial-project disclaimer + estimate-labeling link). Wired into root layout via `SidebarProvider`/`SidebarInset`/`TooltipProvider`. Fixed a lint issue in the generated `use-mobile` hook (setState-in-effect → `useSyncExternalStore`) and two Base-UI API mismatches (`TooltipProvider` uses `delay` not `delayDuration`; `SidebarMenuButton` uses the `render` prop, not `asChild`). Verified: `next build` + `tsc --noEmit` clean, dev server run and HTML fetch confirmed all nav items/header/footer render.
- [x] **3.** 🟢 Implement dark/light mode toggle (next-themes) with persisted preference. Define a devforum-inspired color theme for both modes. ✅ Done — `next-themes` wired via a client `ThemeProvider` (`attribute="class"`, system default, `suppressHydrationWarning` on `<html>`); `ThemeToggle` dropdown (Light/Dark/System) in the header using a hydration-safe `useSyncExternalStore` mounted-check (avoids a lint-flagged setState-in-effect, same trick as the earlier `use-mobile` fix). Devforum-inspired blue accent (Tailwind blue-600 light / blue-400 dark, both in OKLCH) applied to `--primary`/`--ring`/`--sidebar-primary` in both themes, composing with the existing neutral surfaces. Verified: clean `next build`/lint, dev-server fetch confirmed the toggle renders, both theme variants compiled into CSS, and next-themes' anti-flash script is present.

### Phase 1: Data layer

- [x] **4.** 🔴 Set up Prisma. Design schema (DB-agnostic so SQLite-local → hosted Postgres/Turso works): Game, Genre, GameSnapshot (timestamped), GenreSnapshot, GameGenreHistory (genre can change over time), AnalyticsResult tables. Store **all timestamps in UTC**. Track each game's **all-time peak players** (needed for the "dead" rule). Index on gameId, genre, collectedAt. ✅ Done — Prisma **7.8** (new stack: `prisma.config.ts`, `prisma-client` generator → `src/generated/prisma`, **engine-less runtime requires a driver adapter**). Chose the **libSQL adapter** (`@prisma/adapter-libsql`) since it serves BOTH local dev (`file:./dev.db`) and the hosted **Turso** target (Task #30) with only `DATABASE_URL` changing. Schema has all 6 tables **plus** `Theme`/`GameTheme` (locked genre≠theme decision). DB-agnostic guardrails: **no native enums** (String columns + union types in `src/lib/db-constants.ts`), **JSON-as-String** for `AnalyticsResult.payload`, `BigInt` for Roblox ids + visits, no `@db.` native types. Survivorship-bias support: `Game.firstSeenAt`, `allTimePeakPlayers`/`allTimePeakAt`, `status`/`deadSince`. All required indexes present (gameId, genre/currentGenreId, collectedAt) + dedup `@@unique([gameId, collectedAt])`. Client singleton at `src/lib/prisma.ts`; `db:*` npm scripts + `postinstall: prisma generate` (client is gitignored). Migration `init` applied; verified end-to-end via a throwaway Next.js route (write+read+delete round-trip through the real runtime), plus clean `next build`/lint. `dev.db` + `src/generated/` gitignored; generated client excluded from eslint/prettier.
- [x] **5.** 🟡 Genre taxonomy: define our normalized genre list and a mapping layer from Roblox tags → our genres, with manual-override support for mislabeled games. ✅ Done — built `src/lib/taxonomy/`: the 20 normalized gameplay genres (`genres.ts`, typed `GenreSlug` union) + a 14-entry starter theme list (`themes.ts`, kept separate from genre per the locked decision), both seeded into the DB via `prisma/seed.ts` (idempotent upsert-by-slug, wired into `prisma.config.ts` + `npm run db:seed`, using `tsx`). The mapping layer (`genre-mapping.ts`) resolves a game's genre with priority **manual override → name-keyword inference → Roblox genre/tag → null** (never forces a wrong genre), and gathers themes from both the name and Roblox's thematic genres. Covers legacy + modern Roblox genre strings. Manual overrides are a hand-curated per-`universeId` map (`overrides.ts`), persisted downstream as `GameGenreHistory.source="manual"`. Verified the resolver on real examples — e.g. "Anime Fighting Simulator" → genre=Simulator + theme=Anime (the plan's own example), "Tower Defense Simulator" → Tower Defense (TD beats "simulator"), unknowns → null. Clean lint/build; seed loaded 20 genres + 14 themes. **Do NOT use Roblox's official `Genre` enum — it's deprecated and thematic (TownAndCity, Fantasy, Ninja…), not gameplay-based.** Starting normalized list (gameplay genres): Simulator, Tycoon, Obby, RPG, Roleplay & Life, Tower Defense, Fighting/Combat, Shooter/FPS, Horror, Adventure, Survival, Sports, Racing, Clicker/Idle, Strategy, Minigame/Party, Sandbox/Building, Escape/Story, Puzzle, Social/Hangout. Keep **theme tags separate from genre** (e.g. "Anime Fighting Simulator" → genre=Simulator, theme=Anime). List is extensible.
- [x] **6.** 🟡 Build a Roblox API client module (games list, game details, thumbnails, votes/favorites) with rate-limit handling, retries, response caching. ✅ Done — `src/lib/roblox/`. Core HTTP layer (`http.ts`): polite rate limiter (max 4 concurrent, 60ms spacing — datacenter-IP risk noted), retries with exp-backoff honouring `Retry-After` on 429s, per-attempt timeouts (AbortController), an in-process TTL cache, and in-flight request **coalescing**. Typed endpoint wrappers (`client.ts`), all batch calls auto-chunk/dedupe to Roblox's ~100-id limit: `getGameDetails`, `getGameVotes`, `getGameIcons`, `getGameThumbnails`, `getUniverseIdFromPlaceId`, and `searchGames` (keyword discovery via omni-search + a generated sessionId). Types (`types.ts`) confirmed against **live** responses. Key finding: game details carry the legacy `genre` **and** modern `genre_l1`/`genre_l2` — feeding these (most-specific first) into Task #5's resolver correctly yields e.g. Blox Fruits → rpg (via `genre_l1`, better than legacy "Adventure"), Adopt Me → roleplay-life. Verified end-to-end against the real API (discovery, details w/ chunk+dedupe, votes, icons, place→universe) plus cache-hit (0ms vs ~340ms) and coalescing. Clean lint/build. Full discovery _strategy_ (top + new/rising) is assembled on these primitives in Task #8.
- [x] **7.** 🟢 Data validation & sanitization layer — validate/clean Roblox responses before writing (reject nulls/garbage, dedupe) so bad rows never poison analytics. ✅ Done — `src/lib/validation/`, built on **zod 4**. Runtime schemas (`schemas.ts`) for game details, votes, icons, and search results — validating what Task #6's TS types only _assume_ (positive ids, non-negative counts, parseable/consistent timestamps, non-blank names). Sanitizers (`sanitize.ts`) validate + dedupe-by-id per endpoint, returning `{ valid, rejected (with reasons), duplicates }` so a collector run can log/monitor what got dropped (feeds Task #34). `mergeGameData` combines validated details+votes into one clean, DB-ready record (matches Task #4's Game/GameSnapshot fields, extracts `genreSignals` for the Task #5 resolver) — missing votes are **defaulted with a warning, not dropped**, since losing an otherwise-good snapshot over a secondary endpoint hiccup would create gaps in the very history the collector exists to build. Verified against real Task #6 data (passes clean) plus deliberately injected garbage (negative playing, blank name, bad/inconsistent timestamps, non-positive id, exact duplicate) — every case rejected/deduped with the correct reason. Clean lint/build/typecheck.
- [x] **8.** 🔴 Build the data collector. **Scope:** fetch top games AND discover new/rising games near launch, then keep following them as they decline (avoids survivorship bias). Normalize, map genres, write a timestamped snapshot. Run once manually; verify rows land. ✅ Done — `src/lib/collector/` + `scripts/collect.ts` (`npm run collect [-- --max=N --known-only]`). One run: discover → fetch details+votes (Task #6) → sanitize+merge (Task #7) → resolve genre+themes (Task #5) → persist game + timestamped snapshot + genre/theme history (Task #10). **Survivorship-bias guard is implemented** by always re-collecting every game already in the DB (`getKnownUniverseIds`), so games stay tracked as they decline/die — unioned with keyword discovery across the genre space (`searchGames`). One shared `collectedAt` per run; per-game failures are caught+tallied. Verified against the **live API**: run 1 persisted 30 new games (0 rejected, 0 errors, all genres resolved); run 2 (`--known-only`) re-collected the same 30 as updates (0 new, appended 2nd snapshots, 5 peaks rolled) — confirming both insert and update paths. _Note: "new/rising near launch" is currently approximated by search breadth; a dedicated Roblox charts/recent feed is a later refinement — but the critical follow-known mechanism that protects survival stats is in place._
- [x] **9.** 🔴 Implement earnings ESTIMATION model (documented, clearly labeled). ✅ Done — `src/lib/earnings/`. **DevEx rate is the single source of truth** (`devex.ts`), date-aware so historical snapshots use the rate in effect then: 0.0035 pre-2025-09-05, 0.0038 standard from then, plus the separate 0.0054 US-18+ tier from 2026-06-08 (`getDevExRate(date, tier)`, `robuxToUsd`). Estimation model (`estimate.ts`) derives from public signals — CCU and visit-growth — times **tunable assumptions collected in one `EARNINGS_ASSUMPTIONS` const**, converted Robux→USD. Every output is a **labeled `{low, mid, high}` range with `isEstimate: true`** for the UI's "Est." badge — never a hard figure. `deriveSnapshotMetrics(snapshot, prev?)` is the derived-metrics function (likeRatio, CCU earnings, visit-growth earnings). **Design note on "backfill":** estimates are computed **on-read** from stored raw snapshots, so they apply to all history automatically and always reflect the current assumptions/DevEx rate — no stored estimate to go stale or migrate. Verified on real collected data (250k-CCU game → est. ~$7.6k–$42.9k/day, clearly ranged+labeled). **Approach:** Roblox publishes no per-game revenue and gamepass sale counts have been private since July 2020 — so estimate from public signals we can actually collect (concurrent players, visits, visit-growth) × a **configurable revenue-per-metric assumption**, converted Robux→USD via a **DevEx rate stored as a config constant**. Always display as a **labeled range ("Est.")**, never a hard figure. Store the DevEx rate in one place so it's a one-line update when Roblox changes it. DevEx reference: $0.0035 legacy (pre 2025-09-05), $0.0038 standard (from 2025-09-05), $0.0054 US-18+ in-experience (from 2026-06-08). Add a derived-metrics function. Backfill on stored snapshots.
- [x] **10.** 🟡 Build a data-access layer (typed query/repository module) so all pages and analytics read the DB consistently and efficiently — single place to optimize queries. ✅ Done — `src/lib/db/` is the one module that touches Prisma. Write path: `persistCollectedGame` (transactional upsert + snapshot + peak roll + genre-history + theme-sync). Read path: `getGamesList` (filter/sort/paginate), `getGameByUniverseId`, `getGameSnapshots`, `countGames`, plus genre/theme repositories + slug→id maps. **Optimization (standing rule):** added denormalized `current*` metrics + `lastSnapshotAt` to `Game` (migration `add_current_metrics`, indexed) so the core games-list sorts/filters by players/visits/favorites **without joining each row to its latest snapshot** — the collector mirrors them every run. `serialize.ts` (`jsonSafe`/`JsonSafe<T>`) converts Prisma `BigInt`s to strings for RSC→client boundaries. Verified via live-collected data (list, snapshots time series, genre distribution, derived earnings all read correctly).
- [x] **11.** 🟡 Retention & downsampling job. Default policy: keep hourly for 7 days → daily for 90 days → weekly forever (tunable). Keeps the free-tier DB within size limits. **Also add a periodic DB export/backup** (e.g. dump to a file/cloud storage) — accumulated history is irreplaceable and can't be re-fetched. ✅ Done — `src/lib/retention/`. **Downsampling** (`policy.ts` + `downsample.ts`): the bucketing decision is a **pure, tested function** `planDownsample` (hourly ≤7d → daily ≤90d → weekly older; tunable `DEFAULT_RETENTION_POLICY`), keeping the newest snapshot per UTC-day/ISO-week bucket; idempotent. Only loads snapshots past the hourly window (light id/gameId/date projection) — recent hot data untouched. All-time peak lives on Game (not recomputed from snapshots) so downsampling never breaks the "dead" rule. **Backup** (`backup.ts`): logical per-table NDJSON dump + manifest (BigInt→string), which works for both local SQLite and hosted Turso (unlike a .db file copy); paginated for the growing tables. Scripts: `npm run db:backup`, `npm run db:retention` (**backs up first by default**, `--no-backup` to skip). `/backups/` gitignored. Verified: planner unit test (bucketing, latest-in-bucket, idempotency all PASS); live backup of dev.db (155 rows, valid NDJSON, BigInt serialized); downsample correctly no-ops on fresh <7d data.
- [x] **12.** 🟢 ~~Set up scheduled collection locally (cron / node-cron) so history accumulates during development.~~ **CLOSED as obsolete (2026-07-17) — not built, and won't be.** Its entire purpose was "make history accumulate without someone running `npm run collect` by hand", and **Task #32 does exactly that, better**: a GitHub Actions cron collects every 3h against the hosted DB, whether or not this machine is even on. A local node-cron would now be strictly worse — it only runs while a dev machine is awake, and it would write the _same_ rows to the _same_ hosted DB, racing the cloud job for no benefit. Deferring this at the time turned out to be right: the requirement was real, but the plan's assumption that it needed a _local_ scheduler didn't survive Phase 5. **All 40 tasks are now resolved.**

### Phase 2: Core pages (read from DB, not live API)

- [x] **13.** 🟢 Games list page: sortable/filterable table (players, visits, favorites, est. earnings, genre) — the rolimons/games equivalent. Search + pagination. ✅ Done — `/games` (`src/app/games/`). Server-rendered sortable table (players/visits/favorites, click-to-toggle asc/desc via URL params) + genre filter + debounced search (client component, URL-driven) + numbered pagination — all state lives in the URL (shareable/bookmarkable links, no client fetch waterfall). Est. earnings/day shown per row via Task #9's CCU-based estimate (cheap — reuses the already-fetched `currentPlaying`, no extra queries), always with an "Est." badge. Reads exclusively through Task #10's `getGamesList` (denormalized metrics, one query). Verified against the live collected DB: base list shows the correct total (30), `?genre=simulator` correctly narrows to 15 (cross-checked against a direct DB count), search and sort params both return 200 and change results, empty-state message confirmed.
- [x] **14.** 🟢 Game detail page: header stats + historical player-count chart (Recharts) with date-range selector. ✅ Done — `/games/[universeId]` (`src/app/games/[universeId]/`). Header: live-fetched icon (Task #6 client, not persisted in DB), name, genre + theme badges, dead-status badge. Stat tiles: current players, all-time peak, visits, favorites, like ratio, and est. earnings/day (Task #9). Chart: Recharts line, single hue (`var(--primary)`, so it auto-adapts to dark mode), 2px line, hover crosshair + value-leads tooltip, compact Y-axis ticks, no legend (single series — title says what it is) — built per the **dataviz skill**, invoked before writing chart code. A collapsible plain-HTML table under the chart satisfies the skill's "table view must exist" accessibility requirement cheaply. Date-range presets (7D/30D/90D/All) are plain server links (URL param), not client JS. **Cold-start note:** with only ~2 snapshots collected so far the chart is sparse by design (per the plan's own cold-start caveat) — rendering with 2 points was verified to not break. Hit two Recharts-3/Next-16 API specifics along the way: `TooltipContentProps` (not `TooltipProps`) is the type for a custom tooltip renderer, and `content` takes a component reference, not a rendered element. Verified end-to-end against a live collected game: 200 response, icon loaded from the real Roblox CDN, all stat sections present, range param respected.
- [x] **15.** 🟢 Genre statistics page: most played, highest est. earnings, biggest concurrent players, most games — sortable, with date filtering. ✅ Done — `/genres`. Sortable leaderboard (games, players, visits, favorites, est. earnings/day) built on a new `getGenreStats` (`src/lib/db/genre-stats.ts`). Two paths: **"Now"** is a single cheap `groupBy` over Game's denormalized current metrics (same pattern as the games list); a historical **"N days ago"** date filter resolves each game's genre-at-that-instant via `GameGenreHistory` (time-bounded — genre can change) joined to its latest `GameSnapshot` at-or-before that instant, via **one window-function SQL query** (`ROW_NUMBER() OVER (PARTITION BY ...)`) rather than N+1 per-game lookups — standard ANSI SQL, portable to Postgres/Turso. Refactored `SortableHeader` and the range-selector into shared components (`src/components/data-table/`, `src/components/filters/`) since this page needed the identical patterns Task #13/#14 already built — the existing games pages now use the shared versions too. **Verified against live data:** "Now" view's Simulator row shows 15, cross-checked against a direct DB count; `?asOf=7d` correctly returns empty with a clear "collector hasn't reached back that far yet" message (accurate cold-start behavior, not a bug — there's no history from 7 days ago yet since collection only just started); sort params confirmed; the Task #13/#14 pages re-verified still working post-refactor.
- [x] **16.** 🟡 Genre detail page: player trend over time + "lifecycle" chart (avg game player curve from launch to decline). ✅ Done — `/genres/[slug]`. **Prerequisite added:** the collector never wrote `GenreSnapshot`s (its designed home for genre time series), so I added `persistGenreSnapshots` (aggregate per-genre totals/avg/median at each run's `collectedAt`) to the collector — genre trends accumulate from now on. Page has: rollup stat tiles, a **players-over-time** chart from `GenreSnapshot` (shared new `TrendChart` with an optional trailing **moving-average overlay** — uses `movingAverage` in `src/lib/stats.ts`), a **lifecycle** chart (`getGenreLifecycle`: avg players bucketed by weeks-since-launch across the genre's games, computed in JS for SQLite/Postgres portability), and a top-games table. Migrated Task #14's chart onto the shared `TrendChart` (deleted the one-off `PlayerChart`). **Cold-start honesty:** lifecycle is the "basic" descriptive version (rigorous survival/clustering is Phase 4 #21/#23); it renders a cross-sectional age curve now and the empty-state explains sparsity is expected. Verified live on `/genres/simulator`.

### Phase 3: Trends & insights (descriptive)

- [x] **17.** 🟡 Rising/Trending board: fastest-growing games & genres over 7/30 days (growth rate + moving average). ✅ Done — `/trending`. Two boards (rising games, rising genres) with a 7/30-day window toggle. `getRisingGames`/`getRisingGenres` (`src/lib/db/trends.ts`) compute growth = (latest − earliest)/earliest over the snapshots inside the window, picking earliest/latest via correlated MIN/MAX subqueries (portable ANSI SQL, no N+1); games/genres with <2 snapshots in the window are excluded. Growth shown with signed color + `formatGrowthPct`; `movingAverage` util available to smooth (used by the genre trend chart). **Cold-start honest:** with limited history the window growth reflects change over whatever period exists, and the page says so; empty-state handled. Verified live — real movers surfaced (e.g. Pull Up Simulator +60.9%, Zombie Tycoon +40% between runs).
- [x] **18.** 🟡 Basic genre saturation view (games vs. players → crowded vs. under-served). Advanced scoring comes in Phase 4. ✅ Done — `/saturation` (+ sidebar nav link). Scatter of genres: x = number of games (supply), y = players-per-game (demand intensity) — high-left = potentially under-served, right = crowded. Reuses `getGenreStats` ("now"), so it works well even at cold-start (it's a point-in-time view, no history needed). Recharts `ScatterChart` per the **dataviz skill** (single hue, axis labels, per-point hover tooltip, direct genre labels) with a sorted table beneath as the required table-view fallback. `force-dynamic` since it reads live DB aggregates and has no dynamic params (would otherwise be build-time-static/stale). Framed as a signal, not advice; notes the composite opportunity score is Task #24. Verified live — all 4 classified genres plotted + tabled.
- [x] **19.** 🟢 Global date-range control wired across all stats pages (sort by date). ✅ Done — `src/lib/date-range.ts` is now the ONE param convention every stats page shares: query param `range`, values `7d`/`30d`/`90d`/`all`, one meaning ("how far back to look") that each page interprets appropriately — a look-back window on trend charts, "as of N days ago" on point-in-time leaderboards (genres list, saturation), or a growth-comparison window (trending, and new Δ columns). Replaced 3 pages' bespoke ad-hoc params (`asOf` on /genres, `window` on /trending, a locally-duplicated `range` on game detail) with the shared module. **"Sort by date"** is delivered as a **Δ (growth-over-range) column**, scoped to just the rows already on the page via a new `getGrowthForGames` (correlated MIN/MAX subquery, IN-list parameterized via `Prisma.join`) — added to the games list and genre detail's top-games table — rather than rebuilding those lists' whole sort/filter/paginate surface historically, which would have duplicated a lot of query logic for comparatively little gain. Extended `trends.ts` to support `cutoff: undefined` = "all history" (via `Prisma.sql`/`Prisma.empty` conditional fragments). Promoted `GrowthBadge` and `StatTile` to shared components (now used 2–3x identically — genuine reuse, not speculative). Verified live across every page × every range value (200s, real growth %s, correct cold-start empty-states, no fragment-composition bugs in the conditional SQL).
- [x] **20.** 🟢 Dashboard/home page summarizing top insights (trending, top genres, movers). ✅ Done — `/` replaces the Task #1 placeholder. Stat tiles (games tracked, active genres, est. total earnings/day, **last collected** — a lightweight transparency signal via new `getLastCollectedAt`, ahead of Task #34's full monitoring), a top-5-genres panel, and a rising-this-week panel (top games + genres, reusing Task #17/#19's `getRisingGames`/`getRisingGenres` at the 7d cutoff). `force-dynamic` (reads live aggregates, no params to key a static render off). Verified live: real counts (30 games), a real last-collected timestamp, and the same real movers seen on /trending (Pull Up Simulator +60.9%, etc.) — confirming the dashboard and /trending agree, as they must since they share the same query.

### Phase 4: Analytics & data science (precomputed in Python jobs; needs accumulated data)

> Each result is stored in the DB and shown read-only. Estimates/projections are labeled explicitly.

- [x] **21.** 🔴 **Survival analysis (Kaplan-Meier / hazard rates)** — genre lifespan using the "dead" rule (<5% of peak for 7+ days): "median lifespan of a {genre} game is X weeks," handling still-alive (censored) games correctly. Directly answers the core goal. ✅ Done — **`analytics/` Python package established** (venv + lifelines/scikit-learn/pandas; the plan's compute-split: heavy stats precomputed into `AnalyticsResult`, frontend reads only). `analytics/survival.py` fits per-genre Kaplan-Meier (lifelines) via the shared dead-rule detector (`deadrule.py`), right-censoring still-alive games and **left-truncating** at each game's age when first seen (we don't observe from launch — ignoring this biases KM). Writes `survival_km` (per-genre + global). **Cold-start-correct:** with 0 deaths it skips the degenerate/error-prone fit and returns `status:"insufficient_deaths"`; genre detail shows "median lifespan —" + a note that survival needs games followed to death. Verified: jobs run, Prisma reads the Python-written rows (incl. timestamps) cleanly.
- [x] **22.** 🔴 **Trend & momentum metrics** — formalize growth rate, moving averages, and slope of player curves; feeds a robust Rising board and separates real climbs from noise. ✅ Done — `analytics/momentum.py` computes per-game window growth (7d/30d), a trailing moving average, and the **least-squares slope** of the player curve, plus per-genre avg-growth + top-movers. Momentum is anchored on the scale-free growth ratio (not raw slope, which explodes over a tiny observation span). Writes `trend_momentum` (per-game + per-genre); genre detail surfaces avg-7d-growth + the stored top-movers feed a more robust Rising board than raw endpoints. Verified live (34 rows written; real growth values render).
- [x] **23.** 🔴 **Trajectory clustering (k-means / DTW on player curves)** — group games by shape: fast-spike-fast-fade vs. slow-burn-long-tail; surface which pattern a genre tends toward. ✅ Done — `analytics/clustering.py` extracts scale-free curve-shape features (normalized slope, volatility, peak position, last-vs-peak, mean level), standardizes, and runs **k-means (scikit-learn)**, labeling clusters into human archetypes (Rising / Fading / Volatile / Steady) from their centroids. Writes `trajectory_cluster` (per-game archetype + per-genre distribution + global). Genre detail shows the "Trajectory shapes" badge row. Min games/points guard → `status:"insufficient"` when too thin. Verified (30 games → 8 Rising / 22 Steady on current data — crude at cold-start by nature, sharpens as curves lengthen).
- [x] **24.** 🔴 **Opportunity / saturation score** — composite demand-vs-supply index per genre to rank under-served niches (the most decision-relevant feature). Upgrades Task #18. ✅ Done — `analytics/opportunity.py` builds a 0–100 composite from demand intensity (players/game), total demand, short-window momentum, and crowding (games count, penalized), min-max-normalized across genres with tunable `WEIGHTS`. Writes `opportunity_score` (per-genre + a global ranking). **This one is meaningful at cold-start** (reads current aggregates): e.g. Escape/Story ranks #1 (1 game serving ~250k players → under-served), Simulator #3. Upgrades /saturation with an **Opportunity column** (sorts by it) + explanation, and genre detail shows the score + rank. Framed as a descriptive signal, never advice. Verified live end-to-end.
- [x] **25.** 🔴 **Change-point / anomaly detection** — auto-flag player spikes/drops (updates, viral moments) on game and genre curves. ✅ Done — `analytics/anomaly.py` flags steps whose **robust z-score** (median/MAD of successive % changes) exceeds a threshold, so each series is judged against _its own_ typical volatility rather than a global cutoff (MAD, not std, so one big jump doesn't mask the next). Runs on both game player curves and genre total-players curves; writes `change_point` (per-game/genre anomaly lists + a global recent-notable-changes feed). Display: game detail shows a "Notable changes" list (spike/drop badges); /trending shows a global "Recent notable changes" feed. Cold-start guard (needs ≥4 points + non-zero typical variation). Verified live (detected real between-run spikes; renders on both pages).
- [x] **26.** 🔴 **Correlation & feature importance (regression / tree-based)** — "which stats associate with sustained players or higher est. earnings?" Shown as descriptive/associational, NOT causal. ✅ Done — `analytics/correlation.py` combines **Spearman rank correlation** (robust to the heavy-tailed distributions) with **RandomForest feature importance** (catches non-linear/interaction structure) of each stat vs. current player count. Leakage guarded: all-time-peak and playing-derived features are EXCLUDED as predictors; predictors are independent signals (visits, favorites, like ratio, age, votes). Labeled **associational, not causal**, with n reported and "sustained players" noted as a current-snapshot proxy until more history accrues (est. earnings intentionally NOT a target — it's ~linear in playing → circular). Writes `correlation` (global). Display: a dashboard "What's associated with more players" card (correlation bars + note). Verified live (n=30: visits +0.63, votes +0.53, age −0.32 — sensible; RF agrees).
- [x] **27.** 🔴 **Cohort analysis** — group games by launch window; compare retention/decline across genres over time. ✅ Done — `analytics/cohort.py` groups games by launch **quarter** (`robloxCreatedAt`) and summarizes each cohort (games, avg/median current players, avg age, dead count), per genre + global. Writes `cohort`. Display: a "Launch cohorts" table on genre detail. **Works at cold-start** (reads launch dates + current metrics, not long series) and the result is already meaningful: newer cohorts show far higher current players than older ones (e.g. 2026-Q2 ~10.9k avg at 9wk old vs 2025-Q1 ~577 avg at 75wk) — a cross-sectional decline signal, which the UI labels as such. Becomes true per-cohort _retention curves_ once weeks of snapshots accrue.
- [x] **28.** 🔴 **Seasonality detection** — day-of-week / holiday effects on genre popularity. ✅ Done — `analytics/seasonality.py` averages each genre's total-players series by **weekday** (and hour-of-day) and indexes it against the overall mean (1.20 = 20% above average), per genre + global. Writes `seasonality`. Display: a "Weekly seasonality" card on genre detail. **Cold-start-correct:** requires ≥7 distinct days (and ≥12 distinct hours for the hourly view); with <1 day of history every genre returns `status:"insufficient"` and the card says so, reporting how many days we have vs. need. _Holiday effects specifically are deferred — they need a calendar of holidays plus at least a year of history to be anything but noise._
- [x] **29.** 🔴 **Forecasting (Prophet / exponential smoothing)** — near-term genre trajectory projections. Shown as projections WITH uncertainty bands, explicitly labeled — never presented as certain. ✅ Done — `analytics/forecast.py` projects each genre's total-players series with **Holt's linear exponential smoothing**, plus an ~80% uncertainty band derived from in-sample residuals that widens with the horizon (√h) and is floored at 0. Writes `forecast` (per genre). **Chose exponential smoothing over Prophet deliberately** (the plan offers either): Prophet is heavy and wants long daily/seasonal history we don't have — Holt is stable with few points and needs no extra deps; Prophet can be swapped in once weeks of daily snapshots accrue. **Always labeled a projection with a band, never a certainty**, and the payload carries its own caveat text. Guard: ≥5 points → at cold-start (3 genre snapshots) all genres return `status:"insufficient"` and the card says so rather than projecting from noise.

### Phase 5: Deployment & cloud automation (free tier)

- [x] **30.** 🟢 Provision hosted DB (Neon Postgres or Turso); point Prisma at it via env vars. Keep SQLite for local dev. ✅ Done — chose **Turso** (zero code change; `schema.prisma`'s `sqlite` provider + the existing `PrismaLibSql` adapter serve it as-is). `src/lib/prisma.ts` and `prisma/seed.ts` now also read `DATABASE_AUTH_TOKEN`. Since Prisma's schema engine rejects `libsql://` for `migrate deploy` (P1013 — it only recognizes native schemes, not the driver-adapter-only libSQL protocol), migrations against Turso are applied via a new script, `scripts/deploy-migrations.ts` (`npm run db:deploy`), which runs each `migration.sql` through `@libsql/client` directly and records it in a `_prisma_migrations`-shaped table. **Verified live end-to-end**: both migrations deployed, taxonomy seeded (20 genres/14 themes), and a real capped collector run (`--max=10`) persisted 10 games with 0 errors against the hosted DB via the actual `PrismaLibSql` adapter path. Local dev still defaults to `file:./dev.db`; the Turso `DATABASE_URL`/`DATABASE_AUTH_TOKEN` are not committed anywhere — they go into GitHub Actions secrets (#32) and Vercel project env vars (#31) directly.
- [x] **31.** 🟢 Deploy frontend to Vercel/Netlify; configure env/secrets. ⚠️ **Deployed and building, but NOT actually serving the app — needs a Vercel settings fix (found 2026-07-17).** Connected to Vercel from `github.com/Barnadict/rodict` with the Turso DB wired in via project env vars, and builds succeed. But **every app route returns Vercel's _platform_ 404** (`X-Vercel-Error: NOT_FOUND`, generic "The page could not be found" — not our styled `not-found.tsx`), while `/next.svg` — a file in `public/` — returns **200**. That combination is diagnostic: Vercel is publishing this as a **static site and never running Next.js**, so it serves `public/` and no page or Server Component ever executes. **Fix in Vercel → Settings → Build & Deployment: Framework Preset must be "Next.js" (not "Other"), and Output Directory must not be overridden to `public`** (leave it at the framework default). **Why it hid since #31:** Deployment Protection was on, so the URL answered `401` to everyone — the login wall made a broken deployment indistinguishable from a working one, and nobody ever saw past it. Protection is now off (verified: no `401`, no `_vercel_sso_nonce` cookie), which is what exposed this. Re-verify after fixing: `curl https://rodict.vercel.app` should return the dashboard, not "The page could not be found".
- [x] **32.** 🟢 Set up GitHub Actions scheduled workflow to run the collector on a cron against the hosted DB. ✅ Done and verified green — `.github/workflows/collect.yml` (every 3h + manual `workflow_dispatch` with a `knownOnly` toggle) ran successfully against the hosted DB. First real run surfaced a genuine bug: Roblox's `/v1/games` endpoint rejects 100-id batches ("Too many universe IDs were requested", code 9) — every prior local test used `--max=10` so it never hit that boundary. Fixed by lowering `BATCH_SIZE` to 50 in `src/lib/roblox/client.ts` (commit `c8eef5e`), verified with a full 339-game run against the hosted DB (0 errors). Real collection history is now accumulating on Turso via both manual local runs and the scheduled Action.
- [x] **33.** 🟡 Set up a second GitHub Actions (Python) workflow to run the Phase 4 analytics on a schedule and write results to the DB. ✅ Done — `.github/workflows/analytics.yml`. **Triggered by a successful collect run (`workflow_run`) rather than its own cron** (a deliberate deviation from the plan's "on a schedule"): analytics are a pure function of collected data, so running them right after new snapshots land keeps results fresh, can't race the collector, and skips pointless recomputation when no new data arrived. Manual `workflow_dispatch` also available. Required connecting the Python jobs to hosted Turso: `analytics/db.py` now picks its driver from `DATABASE_URL` — stdlib `sqlite3` for `file:`, the `libsql` package for `libsql://` (same DB-API surface, so the jobs are unchanged). **Verified live**: all 9 jobs ran green against the hosted DB. Also fixed `run.py`, which caught every job exception and always exited 0 — a workflow that can't ever go red is worthless as a monitoring signal; it now exits non-zero if any job fails (verified with a simulated failure).
- [x] **34.** 🟡 Collector/analytics failure monitoring — alert on failed runs, log run history, and surface a "last successful collection" indicator in the UI. ✅ Done, all three parts:
  - **Run history** — new `JobRun` table (migration `20260716224142_add_job_run_monitoring`), written by both pipelines (`scripts/collect.ts` and `analytics/run.py`) on success, **partial**, and failure. It exists separately from `Game.lastCollectedAt` because that field only says when data last _landed_, silently conflating "nothing ran" with "the run failed". Per-game errors record as `partial` (data landed, but not cleanly) rather than a clean success. Read layer: `src/lib/db/job-runs.ts`.
  - **Alerting** — `.github/workflows/alert-on-failure.yml`, a reusable workflow both pipelines call on `failure()`. It opens a GitHub issue (needs no external service or extra secret, unlike a Slack/Discord webhook) and **comments on the existing open issue for repeat failures** so a pipeline broken all day leaves one issue with history, not eight duplicates.
  - **UI indicator** — `src/components/layout/data-freshness.tsx` in the site footer (global, since freshness qualifies every number on the site). Distinguishes fresh / stale (>8h — several missed 3h runs, not one blip) / failing / never-ran, and when the pipeline is failing says _"showing data from X ago"_ rather than just "stale", since the displayed data is real, only aging. Uses `connection()` so it's never prerendered — a static footer would freeze the timestamp at build time (this flips `/_not-found` to dynamic; every other route already was).
  - **Verified live** against hosted Turso: both pipelines recorded runs, the footer rendered "Data collected 3 minutes ago", and the failure/partial/truncation paths were unit-tested.

### Phase 6: Polish & quality

- [x] **35.** 🟢 Local watchlist (localStorage, no account) for games/genres. ✅ Done — `src/lib/watchlist/store.ts` is a client-only `useWatchlist()` hook (same `useSyncExternalStore` subscribe/getSnapshot pattern as `use-mobile.ts`) over one `localStorage` key, syncing across components in a tab and across tabs (`storage` event). `WatchlistButton` (`src/components/watchlist/watchlist-button.tsx`) toggles a game/genre and is wired into both list tables (compact icon column) and both detail page headers (labeled button). `/watchlist` resolves the saved ids to live data via `src/lib/watchlist/lookup.ts` — dedicated `"use server"` Server Functions (not part of `src/lib/db`, since these are meant to be called directly from a Client Component) — called from a client view (`watchlist-view.tsx`) that handles loading/empty/error, and shows a "no longer tracked" row rather than silently dropping an entry whose game/genre disappeared from collection. No account, nothing server-persisted.
- [x] **36.** 🟢 Loading states, empty states, error handling, mobile responsiveness, and a basic accessibility (a11y) pass. ✅ Done:
  - **Loading** — a `loading.tsx` per route (`/`, `/games`, `/games/[universeId]`, `/genres`, `/genres/[slug]`, `/trending`, `/saturation`, `/watchlist`), built from two new shared skeletons (`TableSkeleton`, `StatTilesSkeleton`) that roughly match each page's real layout. Verified live — Next streams the fallback markup before the resolved content in the same response (confirmed both are present in a raw `curl`, i.e. these are real Suspense boundaries, not dead code).
  - **Empty states** — already solid from earlier tasks (every list/chart had a cold-start empty message); added the watchlist's own empty state as the one gap.
  - **Error handling** — root `error.tsx` (uses Next 16.2's `unstable_retry`, the replacement for the old `reset()` — a real API difference caught by reading `node_modules/next/dist/docs` per `AGENTS.md` rather than assumed from training data), `global-error.tsx` for root-layout crashes, and a custom `not-found.tsx` (verified live: an unknown `universeId` renders the styled 404, not Next's default).
  - **Mobile** — audited the two page headers this task touched (game/genre detail, now with a watchlist button beside the title) and added `flex-wrap`/`min-w-0` so a long name plus the button stacks instead of overflowing on narrow widths; everything else (collapsible-to-Sheet sidebar, `overflow-x-auto` tables, `sm:` grid breakpoints) was already in place from earlier tasks.
  - **A11y** — labeled the two placeholder-only search inputs (`aria-label`, header search + games filter), added `role="img"`/`aria-label` to the three Recharts wrappers (their SVGs otherwise carry no text for screen readers), sr-only sort-direction text on `SortableHeader`, and `aria-pressed`/`aria-label` on the new icon-only watchlist toggles.
  - Verified: clean `tsc --noEmit`, `eslint`, `next build`, and a live dev-server pass (curl smoke tests across all routes + a real game/genre + a 404 case).
- [x] **37.** 🟡 Testing — cover the earnings-estimation model, the analytics functions (correctness), and the data-validation layer. ✅ Done — **149 tests across two suites**, both pure-function only (no DB, no network, no fixtures to keep in sync):
  - **Vitest** (`npm test`, 73 tests) — the framework Next's own testing guide documents. Set up leaner than that guide in two ways, both deliberate: no `@vitejs/plugin-react`/jsdom (nothing renders — the guide itself says async Server Components belong in E2E, and every page here is one, so the suite targets the pure logic those pages call), and no `vite-tsconfig-paths` (Vitest 4 warns it's redundant; Vite resolves the `@/*` alias natively now — the guide predates that). Covers the **earnings model** (`devex.test.ts`, `estimate.test.ts` — DevEx rate schedule boundaries, band ordering, the always-`isEstimate` flag, per-day normalization, div-by-zero guards), the **validation layer** (`sanitize.test.ts` — every documented rejection reason, dedupe, and the deliberate keep-the-game-warn-on-missing-votes merge rule), and the **analytics helpers** (`stats.test.ts`, `policy.test.ts` — incl. the retention planner's idempotency and ISO-week year boundary, which Task #11 had only checked with a throwaway script).
  - **pytest** (`npm run test:py`, 76 tests) — the Phase 4 statistics are Python, so testing "analytics correctness" without them would have missed the point. Covers the locked **dead rule** (threshold strictness, the 7-day span boundary, recovery resetting the run), **momentum**, **opportunity** (min-max, weight signs), **survival** (censoring + the left-truncation guard), **seasonality**, and **anomaly** detection.
  - **Found and fixed a real user-facing bug in anomaly detection (#25).** With only a robust z-score, a step merely had to be unusual _for its own series_ — but on a very regular curve the typical step is tiny, so MAD collapses and an ordinary 2% wiggle scores z=36. Against the real collected data, **11 of 15 flagged "notable changes" were noise** (a −3.4% drop; a +7.9% move at z=149). Fixed by requiring a flag to clear two independent bars — statistically unusual **and** materially large (`MIN_ABS_CHANGE`) — verified to suppress all 11 while still catching a genuine +165% spike, with regression tests pinning both directions. Also closed the same root cause's other failure mode: the tail of a smooth decline being reported as a series of drops when nothing had actually happened.
  - **Also fixed:** `npm run analytics` had been **broken on Windows since Task #21** (forward slashes in the leading command token, which cmd.exe can't resolve) — surfaced because the new `test:py` script hit it too. CI never caught it since the workflow calls `python analytics/run.py` directly.
  - **CI** — a new `.github/workflows/test.yml` runs both suites (+ typecheck and lint) on every push/PR; needs no secrets since nothing touches the DB. Workflow name quoted, and re-verified by parsing the YAML that all four workflow names still resolve in full (the Task #33 `#`-truncation trap).
  - **⚠️ Follow-up (2026-07-17): this workflow's first real run went red**, on its `tsc --noEmit` step — the one thing that couldn't be verified when it was written, since it had never executed on GitHub. `PageProps`/`LayoutProps` are **globals Next generates into `.next/types`**, which is gitignored; bare `tsc` therefore passes locally (a build has run) and fails on a clean checkout with `Cannot find name 'PageProps'` ×7. Reproduced exactly by moving `.next` aside. Fixed with a `typecheck` script — `next typegen && tsc --noEmit` — which is the recipe Next's own CLI docs give for "CI type-checking of route usage", and which needs no `DATABASE_URL` (unlike a full build, which would prerender and hit the DB). pytest was green throughout.
- [x] **38.** 🟢 "About the data" page — data sources, collection method, definitions, and explicit explanation that earnings/forecasts are estimates. ✅ Done — `/about` (the footer and sidebar had linked to it since Task #2; it 404'd until now). Covers the four required parts plus limitations and the descriptive-not-prescriptive stance, with the estimates disclosure pulled to the top in a callout rather than buried, since it's the single most important thing on the page. **Every number is imported, not retyped** — the DevEx rate schedule (newly exported as `DEVEX_SCHEDULES` for this), `EARNINGS_ASSUMPTIONS`, `DEFAULT_RETENTION_POLICY`, and the genre/theme counts all come from the constants the app actually computes with, so the page cannot drift into describing assumptions that are no longer true. Documents what the estimate explicitly _cannot_ know (per-game monetization, real gamepass revenue, Roblox's cut, whether the dev ever cashes out), the ToS/attribution stance from the Risks section, and the cold-start caveats. Reads no DB, so it costs nothing per request.
- [x] **39.** 🟡 Performance optimization pass — audit with Lighthouse/bundle analyzer, profile slow DB queries, verify indexes, tighten caching and code-splitting. (Reinforces the standing rule; does not replace optimizing along the way.) ✅ Done — **the headline: every route was `ƒ Dynamic`, re-querying the DB on every request to render numbers the collector only changes every 3h.** Even `/about`, which reads no DB at all, was dynamic — because the footer's freshness indicator calls `connection()`, and under the old model one request-time API anywhere makes the whole route dynamic.
  - **Adopted Cache Components** (`cacheComponents: true`). This is the Next 16 answer, not a preference: `unstable_cache` is explicitly _"replaced by `use cache`"_ in the docs, so the alternative was building on a deprecated API. Every route is now `◐ Partial Prerender` — static shell served instantly, only genuinely per-request content streams. Each page's reads live in a `use cache` loader with `cacheLife("hours")` beside the page that owns them.
  - **The trap worth remembering: cache on the range _key_, never a `Date`.** A cutoff derived from `now` and passed in as an argument is a fresh cache key every single request — the cache would never hit while still growing. Every loader takes `RangeKey` and derives the cutoff inside. **Verified empirically**, not assumed: a probe log showed 6 requests → 2 executions (4× default range → 1 run, 2× `?range=30d` → 1 run), i.e. exactly one per distinct key.
  - **Found a real index gap.** `currentFavorites` is user-sortable on the games list but was the only one of the five `GameSortField`s without an index — `EXPLAIN QUERY PLAN` confirmed `SCAN Game | USE TEMP B-TREE FOR ORDER BY` while its four siblings used indexes. Added `@@index([currentFavorites])` (migration `add_current_favorites_index`) and re-verified the plan now uses it. ⚠️ **Needs `npm run db:deploy` to reach Turso** — local `.env` has no Turso credentials by design, so this is on you.
  - **Bundle:** measured per-route rather than guessed — ~272 KB gzipped on chart-less routes vs ~377 KB on chart routes, so **Recharts (~105 KB) is already code-split** and only loads where a chart renders. No change warranted; lazy-loading a chart that _is_ the page's content would just delay the content.
  - **Roblox API traffic:** game detail was calling the live Roblox icon endpoint on every page view. It's now inside the cached loader — that's the standing "cache Roblox API responses" rule, and it matters because the API is rate-limited and semi-official.
  - **Investigated and cleared, not a bug:** not-found pages return HTTP 200 rather than 404. Next documents this — streamed responses return 200 because headers flush before the body resolves — and it dates to Task #36's `loading.tsx`, not this task. Verified it's benign: Next injects `<meta name="robots" content="noindex">` on the streamed 404 (confirmed present there and absent on real pages), which is what stops soft-404 indexation.
  - **Left deliberately:** caches are in-memory. `use cache: remote` is the upgrade path if Turso reads ever bite, but the docs advise against it exactly where we'd need it (search-filtered views have near-unique keys), so it would mostly miss.
- [x] **40.** 🟢 Final review: caching, cleanup, and write README with run steps. ✅ Done — README gained a **Rendering & caching** section (the PPR model, the cache-on-the-key rule, why the freshness indicator stays live, and the `use cache: remote` upgrade path), and the deploy section now says plainly that `npm run db:migrate` does **not** reach Turso — `npm run db:deploy` is required after any schema change, which is exactly the trap #39's new index falls into. `next.config.ts` documents why Cache Components is on. Cleanup: no `TODO`/`FIXME` left in first-party code, no stray probe scripts, no leftover `force-dynamic`/`unstable_cache`. **Caught a real rendering bug while verifying:** `/about` rendered "20of them" — under Turbopack, a JSX text chunk following an expression loses its leading space when it wraps across lines (single-line chunks like `Up to {n} days` are fine), and Prettier reformats a `{" "}` fix away. Fixed by keeping value and unit in one expression, with a comment so the next person doesn't "tidy" it back. Verified: clean `tsc`/`eslint`/`prettier --check`, 149 tests green, clean build (all 10 routes `◐`), and a live pass over every route.

- [x] **41.** 🔴 Scale the tracked corpus from a few hundred to a few thousand games. ✅ Done — the corpus had plateaued at ~343 because discovery took only **page 1 of omni-search, then sliced it to 20 results per query** (`.slice(0, 20)`), and never followed `nextPageToken` (declared in the types, used nowhere). One page is ~40 games; a query has hundreds behind the token. Probing the live API: the same 20 keywords reach **~4,800 unique games** paginated vs. 343 before — the old cap was exactly "what one shallow page reaches", which is why re-running never grew it. **Three parts:** (1) `searchGames` now walks up to `maxPages` (default 10, stops early on an empty page); discovery also pulls the **explore-api charts** (Top Playing Now, Up-and-Coming, …) — the real Roblox charts, and Up-and-Coming surfaces games _near launch_, which keyword search misses and the survivorship guard most wants. (2) **The write path was rebuilt from one interactive transaction per game (~6 round trips each → ~26 min at 5k, over the timeout) to bulk set-operations** (`persistCollectedGames`): one pre-read, chunked `createMany`s, batched `$transaction([...])` updates, genre/theme diffed in memory. The pure decision logic (`planGameWrite`) is unit-tested (12 cases). (3) **Scaling exposed a fetch fragility the small corpus hid** — ~5k games means ~170 batch requests to the strict `games.roblox.com`, which starts returning 429; worse, one exhausted batch used to abort the _whole_ run. Fixed both: gentler global pacing (2-concurrent / 200ms, was 4 / 60ms), more retries (8 attempts on that host, 20 s backoff cap), details fetched **before** votes instead of in parallel (same host — halves peak load), and a failed batch is now **skipped and counted as a partial run**, never fatal — the survivorship guard re-collects the gap next run. **Verified end-to-end against a scratch copy of the local DB: 30 → 4,188 games in ~6 min (well under the 30-min budget), 0 duplicate snapshots, exactly one open genre-history row per game (invariant held with no `skipDuplicates`, which SQLite's Prisma `createMany` doesn't support anyway).** Collector workflow timeout raised 15 → 30 min for headroom.

### Later (out of scope for now)

- Accounts, auth/verification, ads.
- **Update/relaunch impact** — correlate player spikes with detected game updates (builds on Task #25).

---

## Data-science notes

- **All Phase 4 methods need accumulated history** — results are thin until the collector has gathered weeks/months of snapshots. Start collection (Task #8/#12) early.
- **Estimates stay estimates** — earnings-derived and forecasted results inherit uncertainty and are labeled as such everywhere they appear.
- **Descriptive, not prescriptive** — everything shows patterns and lets the developer decide; the site never tells them what to build.
- **Compute split** — heavy stats run offline in Python (scikit-learn, lifelines, Prophet/statsmodels) as scheduled jobs and are precomputed into the DB; the frontend only displays results.

---

## Progress log

_(Claude appends a one-line note here each time a task is completed.)_

- _Plan created 2026-07-16._
- _2026-07-16: Expanded plan — added full analytics phase (survival analysis, clustering, opportunity scoring, anomaly/change-point, correlation, cohort, seasonality, forecasting), a free-tier hosting + GitHub Actions automation phase, and explicit estimate-labeling requirements._
- _2026-07-16: Added performance & optimization as a standing cross-cutting requirement plus a dedicated optimization pass._
- _2026-07-16: Locked 3 design decisions (collection scope = top + follow new; "dead" = <5% of peak for 7+ days; genre = normalized mapping). Added data validation, data-access layer, retention/downsampling, collector monitoring, testing, UTC storage, a Risks section, a Definitions section, and a cold-start note. Plan now 40 tasks across 7 phases._
- _2026-07-16: Final review — folded in git/GitHub setup (Task #1), DB export/backup for irreplaceable history (Task #11 + Risks), and .env.example/accessibility notes. Task count unchanged (folded into existing tasks, no renumbering)._
- _2026-07-16: Added "Prerequisites you must provide" section with a reminder protocol; locked site name = rodict, hosting = Vercel, collection frequency = default hourly, custom domain skipped. Earnings assumptions (#6) + genre list (#7) researched via web — pending user confirmation before adding to Tasks #9/#5._
- _2026-07-16: User approved both — normalized genre list written into Task #5, earnings-estimation approach + DevEx-rate constant written into Task #9, and both reflected in Definitions._
- _2026-07-16: Added per-task model hints (🟢 Sonnet · 🟡 borderline · 🔴 Opus) after each task number, with a legend under the task-list header. Every task is explicitly marked. User switches models manually per task._
- _2026-07-16: **Task #1 complete.** Scaffolded Next.js 16 + React 19 + Tailwind v4 (App Router, src/, @/\* alias) as package "rodict"; added Prettier (+ eslint-config-prettier), .env.example, .gitattributes; created src/components & src/lib; production build verified clean; git repo initialized with 2 commits. GitHub remote intentionally deferred to Task #32. **Next: Task #2** (shadcn/ui + devforum-style base layout)._
- _2026-07-16: **Task #2 complete.** shadcn/ui installed (base-nova style, Base UI primitives). Built devforum-style base layout: collapsible left sidebar (Dashboard/Games/Genres/Trending/Watchlist/About), top header (sidebar toggle + search), footer (unofficial-project + estimates disclaimer). Root layout wired with SidebarProvider/TooltipProvider. Fixed a generated-hook lint error and two Radix→Base-UI API differences encountered along the way. Verified via clean `next build`/`tsc --noEmit` and a live dev-server HTML check. **Next: Task #3** (dark/light mode toggle)._
- _2026-07-16: **Task #3 complete.** Added next-themes with a class-based ThemeProvider (system default) and a Light/Dark/System dropdown toggle in the header, using a hydration-safe useSyncExternalStore pattern instead of a setState-in-effect mounted check. Defined a devforum-inspired blue accent (Tailwind blue-600/blue-400 in OKLCH) for --primary/--ring/--sidebar-primary in both themes. Verified clean build/lint and confirmed via dev-server fetch that both theme variants compiled and the anti-flash script is present. **Next: Task #4** (Prisma schema)._
- _2026-07-16: **Task #4 complete.** Set up Prisma 7.8 (engine-less; uses prisma.config.ts + the new prisma-client generator + a driver adapter). Picked the libSQL adapter so local SQLite and hosted Turso share one code path. Designed the full DB-agnostic schema (Game, Genre, Theme, GameTheme, GameGenreHistory, GameSnapshot, GenreSnapshot, AnalyticsResult) — String-based "enums" + src/lib/db-constants.ts, JSON-as-String payloads, BigInt ids/visits, UTC datetimes, all-time-peak + firstSeenAt for survivorship-bias/"dead"-rule support, and the required indexes. Added a Prisma client singleton, db:\* scripts, and postinstall generate. Applied the init migration and verified a full write/read/delete round-trip through the Next.js runtime. **Next: Task #5** (genre taxonomy + Roblox-tag mapping)._
- _2026-07-16: **Task #5 complete.** Built src/lib/taxonomy/: 20 normalized gameplay genres + 14 starter themes (typed slug unions), seeded into the DB idempotently (prisma/seed.ts via tsx, wired into prisma.config.ts + db:seed). Mapping layer resolves genre by priority manual-override → name-keyword → Roblox-tag → null (never forces a wrong genre) and detects themes from name + Roblox thematic genres; covers legacy + modern Roblox genre strings. Manual overrides = per-universeId map, persisted downstream as GameGenreHistory source="manual". Verified resolver on real games (e.g. "Anime Fighting Simulator" → Simulator + Anime). Clean lint/build. **Next: Task #6** (Roblox API client)._
- _2026-07-16: **Task #6 complete.** Built src/lib/roblox/ — a typed Roblox API client with a shared HTTP core (rate limiting, Retry-After-aware retries + backoff, timeouts, in-process TTL cache, request coalescing) and auto-chunking batch wrappers for details/votes/icons/thumbnails/place→universe, plus searchGames (omni-search discovery). Confirmed shapes against the live API; discovered games expose modern genre_l1/genre_l2 alongside legacy genre, which feed Task #5's resolver correctly. Verified end-to-end against the real API incl. cache-hit (0ms) and coalescing. Clean lint/build. **Next: Task #7** (data validation/sanitization layer)._
- _2026-07-16: **Task #7 complete.** Added zod-based runtime schemas + sanitizers (src/lib/validation/) validating Roblox details/votes/icons/search results, with per-endpoint dedupe-by-id and a mergeGameData helper producing clean DB-ready records (missing votes default with a warning rather than dropping the game). Verified against real Task #6 data plus deliberately injected garbage — every bad row rejected with the correct reason, duplicates removed. Clean lint/build/typecheck. **Next: Task #8** (the data collector)._
- _2026-07-16: **Tasks #27–#29 complete — Phase 4 (analytics & data science) DONE.** All 9 Python jobs now run via `npm run analytics`. #27 cohort: games grouped by launch quarter w/ per-cohort avg players + age (per genre + global) — works at cold-start and already shows the newer-cohorts-have-more-players decline signal; shown as a "Launch cohorts" table on genre detail. #28 seasonality: weekday/hour indices vs the mean (needs ≥7 distinct days → currently insufficient, card says so; holiday effects deferred until a year of history + a holiday calendar). #29 forecast: Holt exponential smoothing + ~80% band widening with horizon, always labeled a projection (chose Holt over Prophet — stable with few points, no heavy deps; Prophet swappable later); needs ≥5 points → currently insufficient. Verified live: cohort table renders real quarters (2020-Q2..2026-Q2), seasonality/forecast show honest cold-start states. Clean lint/build. **Next: Phase 5 — Task #30** (hosted DB) or #12._
- _2026-07-16: **Tasks #25 and #26 complete.** #25 change-point/anomaly detection (analytics/anomaly.py): robust median/MAD z-score on % changes flags spikes/drops on game + genre curves; shown as a "Notable changes" list on game detail and a global "Recent notable changes" feed on /trending. #26 correlation & feature importance (analytics/correlation.py): Spearman + RandomForest of independent stats vs player count (leakage-guarded, labeled associational-not-causal), shown as a dashboard card. Both wired via src/lib/db/analytics.ts and cold-start-graceful. Verified live (real anomalies; visits/votes top correlates). Clean lint/build. **Next: Task #27** (cohort analysis) or #12._
- _2026-07-16: **Tasks #21–#24 complete — Phase 4 analytics scaffolding online.** Established the `analytics/` Python package (venv, lifelines + scikit-learn + pandas) per the plan's compute-split: heavy stats precomputed into AnalyticsResult, the Next.js frontend reads only (new src/lib/db/analytics.ts). Confirmed Python↔Prisma interop (ISO-ms timestamps, BigInt). **#21** survival: per-genre Kaplan-Meier w/ dead-rule + censoring + left-truncation (cold-start → insufficient_deaths, handled). **#22** momentum: growth/MA/curve-slope per game+genre. **#23** clustering: k-means archetypes on scale-free curve-shape features. **#24** opportunity: composite demand-vs-supply score (meaningful now — Escape/Story #1). Wired into /saturation (Opportunity column) + genre-detail Insights section, all cold-start-graceful. `npm run analytics` runs all jobs. Clean lint/build; verified live. **Next: Task #25** (change-point/anomaly detection) or #12._
- _2026-07-16: **Tasks #19 and #20 complete — Phase 3 done.** #19: unified every stats page onto one date-range convention (src/lib/date-range.ts: range=7d/30d/90d/all, one cutoff meaning per-page-appropriately interpreted); delivered "sort by date" as scoped Δ-growth columns (new getGrowthForGames) on games list + genre top-games rather than a full historical list rebuild; extended trends.ts to support "all history" via conditional Prisma.sql/Prisma.empty fragments; promoted GrowthBadge/StatTile to shared components. #20: built the real dashboard (stat tiles incl. last-collected, top genres, rising-this-week), force-dynamic. Verified every page × every range value live, and confirmed the dashboard's movers match /trending's exactly. Clean lint (0 warnings)/build/typecheck. \**Next: Task #12 (whenever) or Phase 4 (Task #21, survival analysis)._
- _2026-07-16: **Tasks #16–#18 complete** (Phase 3 descriptive trends). Added GenreSnapshot writing to the collector (persistGenreSnapshots) so genre time series accumulate — a prerequisite the earlier collector lacked. **#16** /genres/[slug]: genre players-over-time trend (shared new TrendChart with moving-average overlay), a basic lifecycle curve (avg players by weeks-since-launch, JS-computed for portability), and top games; migrated Task #14 onto the shared TrendChart. **#17** /trending: rising games & genres over a 7/30-day window (growth via correlated MIN/MAX subqueries, no N+1) with signed growth + MA util. **#18** /saturation: genre supply-vs-demand scatter (games vs players-per-game) + table, dataviz-compliant, force-dynamic. All cold-start-graceful and verified live (real movers like +60.9%; genre-filtered counts; scatter). Clean lint/build/typecheck. **Next: Task #12 (whenever) or Task #19/#20** (global date-range control / dashboard)._
- _2026-07-16: **Task #15 complete.** /genres leaderboard (games/players/visits/favorites/est. earnings, sortable) via a new getGenreStats: a cheap groupBy for "Now", and a single window-function SQL query (GameGenreHistory join latest-GameSnapshot-at-or-before-date) for historical "N days ago" filtering — avoids N+1, portable SQL. Promoted SortableHeader and the pill-button date/range selector into shared components, refactoring Tasks #13/#14 to use them too. Verified: Simulator's 15-game count matches the DB directly; ?asOf=7d correctly shows a cold-start empty-state (no history that far back yet) rather than erroring or showing zeros confusingly; existing pages re-verified post-refactor. Clean lint/build/typecheck. **Next: Task #12 (whenever) or Task #16** (genre detail page — the /genres/[slug] links this page already points to)._
- _2026-07-16: **Tasks #13 and #14 complete** (Task #12 — local scheduled collection — intentionally skipped for now at user's request; manual `npm run collect` still works meanwhile). **#13** /games: server-rendered sortable/filterable table (players, visits, favorites, genre, est. earnings), URL-driven search/genre-filter/pagination, reading through the Task #10 data-access layer. **#14** /games/[universeId]: header stats + a Recharts player-count chart (dataviz-skill-compliant: single brand hue, crosshair tooltip, no legend, collapsible table-view fallback) with a 7D/30D/90D/All range selector; live-fetches the game icon since it isn't persisted in the DB. Both verified against the real collected DB (genre-filtered count cross-checked against a direct query; detail page confirmed loading a live Roblox icon). Clean lint/build/typecheck. **Next: Task #12** (whenever) **or Task #15** (genre statistics page)._
- _2026-07-16: **Task #11 complete.** Retention/downsampling (src/lib/retention/) with a pure, unit-tested planDownsample (hourly≤7d → daily≤90d → weekly; idempotent) applied via downsampleSnapshots, plus a DB-agnostic logical NDJSON backup (works for SQLite + Turso). Scripts db:backup / db:retention (backs up before deleting). Verified planner logic + live backup (155 rows) + no-op downsample on fresh data. **Next: Task #12** (local scheduled collection)._
- _2026-07-16: **Tasks #8–#10 complete** (built together in dependency order). **#10 data-access** (src/lib/db/): the single Prisma-facing module — transactional collector write path + list/detail/snapshot reads; added denormalized current\* metrics to Game (migration add_current_metrics) so the games-list needs no per-row snapshot join; BigInt→string serialize helpers. **#9 earnings** (src/lib/earnings/): date-aware DevEx-rate single-source-of-truth + a tunable CCU/visit-growth estimation model returning labeled {low,mid,high} ranges (isEstimate), computed on-read (no stale stored estimates / no backfill needed). **#8 collector** (src/lib/collector/ + scripts/collect.ts): discovery (follow-known + keyword search) → fetch → sanitize → resolve → persist; survivorship-bias guard via always re-collecting known games. **The collector has now run against the live API — real history is accumulating** (30 games, 2 snapshots each; insert + update paths both verified). Clean lint/build/typecheck. **Next: Task #11** (retention/downsampling + backup)._
- _2026-07-16: **Tasks #30–#34 complete — Phase 5 (deployment & cloud automation) done.** Provisioned hosted Turso DB (zero code change — existing PrismaLibSql adapter serves it) and worked around Prisma's schema engine rejecting `libsql://` for migrations (`scripts/deploy-migrations.ts`, applies migration.sql directly via `@libsql/client`). Deployed to Vercel. GitHub Actions collector cron (every 3h) — a real run surfaced Roblox's actual ~50-id batch limit (prior local tests never hit it), fixed and reverified with a full 339-game run. A second Python analytics workflow, triggered by the collector's success (`workflow_run`, not its own cron — analytics are a pure function of the data collect just wrote) rather than the plan's literal "on a schedule". Failure monitoring: a `JobRun` table (recorded by both pipelines, success/partial/failure), a reusable `alert-on-failure.yml` that opens/comments on a GitHub issue, and a footer "last successful collection" indicator (`connection()`-gated so it's never prerendered). Along the way: fixed `run.py` always exiting 0 (a monitoring workflow that can't go red is worthless), and an unquoted `#` in a workflow `name:` that silently truncated it and broke `workflow_run`'s name-match — caught by parsing the YAML rather than trusting the read. Clean lint/build; verified live end-to-end against the hosted DB. **Next: Phase 6 (Task #35, local watchlist)**, or #12._
- _2026-07-17: **Tasks #35 and #36 complete — Phase 6 underway.** **#35** local watchlist: `src/lib/watchlist/store.ts` (`useSyncExternalStore` over one localStorage key, the same pattern as `use-mobile.ts`), a `WatchlistButton` wired into both list tables and both detail headers, and `/watchlist` resolving saved ids to live data through dedicated `"use server"` Server Functions (`src/lib/watchlist/lookup.ts`) called from a client view with its own loading/empty/error/"no-longer-tracked" states — no account, nothing server-persisted. **#36**: a `loading.tsx` per route built from two new shared skeletons, matching Next 16.2's real streaming behavior (verified: both the fallback and resolved markup appear in a raw `curl`, confirming these are live Suspense boundaries); root `error.tsx` (using Next 16.2's `unstable_retry` — a real API difference from the `reset()` prop trained knowledge would assume, caught by reading `node_modules/next/dist/docs` per `AGENTS.md`), `global-error.tsx`, and a custom `not-found.tsx` (verified live on an unknown universeId); `flex-wrap`/`min-w-0` fixes on the two headers this task touched so a long name plus the new watchlist button stacks instead of overflowing on narrow widths; and an a11y pass (labeled the two placeholder-only search inputs, `role="img"`/`aria-label` on the three Recharts wrappers, sr-only sort-direction text, `aria-pressed` on the watchlist toggles). Verified: clean `tsc`/`eslint`/`next build`, plus a live dev-server pass (curl smoke tests across every route, a real game/genre, and a 404 case). **Next: Task #37** (testing) or #12._
- _2026-07-17: **BUILD COMPLETE — all 40 tasks resolved; project paused to accumulate history.** Verified live in production: every route 200s, `X-Nextjs-Prerender: 1` confirms #39's PPR/caching is real in prod, and the footer's live-streamed "Data collected 1 hour ago" confirms the cached-data/live-freshness split works. 380 games, 18 genres. The `currentFavorites` index reached Turso via the new `migrate.yml` workflow. **#12 closed as obsolete** — a local node-cron would only run while a dev machine is awake and would race #32's cloud cron writing the same rows to the same DB; deferring it was right, and the requirement was met a better way. **Also fixed: `test.yml`'s first-ever run on GitHub went red.** `PageProps` is a global Next generates into the gitignored `.next/types`, so bare `tsc --noEmit` passes locally (a build has run) and fails on a clean checkout — 7× `Cannot find name 'PageProps'`, reproduced by moving `.next` aside. Replaced with a `typecheck` script (`next typegen && tsc --noEmit`), the recipe Next's CLI docs give for CI, which needs no DATABASE_URL. CI green. That's twice in one session that a workflow's first real run caught what no local verification could. **Paused deliberately:** survival/seasonality/forecast still report `insufficient` and only weeks of snapshots will change that — the cold-start caveat working as designed, not a bug._
- _2026-07-17: **Tasks #38–#40 complete — the plan is done (except the skipped #12).** **#38** `/about`: the page the footer and sidebar had linked to since Task #2 and which had been 404ing ever since. Every figure is imported from the constants the app computes with (DevEx schedule — newly exported for this — earnings assumptions, retention policy, genre/theme counts) rather than retyped as prose that drifts; estimates disclosure sits at the top, not buried. **#39** performance: found every route was `ƒ Dynamic`, re-querying the DB per request for numbers that change every 3h — even `/about`, which reads no DB (the footer's `connection()` made the whole route dynamic). Adopted **Cache Components** (`use cache` + PPR — the docs say `unstable_cache` is "replaced by `use cache`", so the alternative was a deprecated API); every route is now `◐ Partial Prerender`. The trap: **cache on the range key, never a `now`-derived `Date`**, or the key changes every request and never hits — verified with a probe (6 requests → 2 executions, one per distinct key). Verified indexes with `EXPLAIN QUERY PLAN` and found a real gap: `currentFavorites` was sortable but unindexed (full SCAN + temp B-tree) while its 4 siblings were fine — indexed and re-verified. Measured the bundle rather than guessing: Recharts (~105 KB gzipped) is already code-split to chart pages only, so no change. Also put the per-view live Roblox icon call behind the cache. Investigated the 200-on-404: documented Next behavior for streamed responses, dating to #36's `loading.tsx`, and benign because Next injects `noindex` (confirmed). **#40** review: README gained a Rendering & caching section and an explicit "db:migrate doesn't reach Turso" warning; cleanup clean; caught `/about` rendering "20of them" — a Turbopack JSX whitespace quirk where a wrapped text chunk after an expression loses its leading space, and Prettier undoes the `{" "}` fix. Clean tsc/eslint/prettier, 149 tests, clean build, live pass over every route. **Open for the user: `npm run db:deploy` for the new index; #35–#40 are uncommitted.**_
- _2026-07-17: **Task #41 complete — corpus scaled ~343 → thousands.** Discovery had plateaued not because Roblox ran out of games but because it took only page 1 of omni-search sliced to 20 results/query and never followed `nextPageToken` (declared in the types, used nowhere) — 343 was exactly "what one shallow page reaches", so re-running never grew it. Now `searchGames` paginates (up to 10 pages, early-stops on empty) and discovery also pulls the **explore-api charts** (Top Playing Now, Up-and-Coming, …); the same 20 keywords reach ~4,800 unique games. **The write path was rebuilt from one interactive transaction per game (~26 min at 5k — over the timeout) to bulk set-operations** (`persistCollectedGames`: one pre-read, chunked `createMany`s, batched `$transaction` updates, in-memory genre/theme diffs), with the pure decision logic (`planGameWrite`) unit-tested (12 new tests → 161 total). **Scaling exposed a fetch fragility the small corpus hid:** ~170 batch requests to the strict `games.roblox.com` start drawing 429s, and one exhausted batch used to abort the whole run — fixed with gentler pacing (2/200ms, was 4/60ms), 8 attempts on that host, details-before-votes (same host, halves peak load), and per-batch fault tolerance (a failed batch is skipped and reported as a partial run, never fatal; the survivorship guard re-collects it next run). **Verified end-to-end on a scratch DB copy: 30 → 4,188 games in ~6 min, 0 duplicate snapshots, exactly one open genre-history row per game.** Timeout raised 15 → 30 min. Clean tsc/eslint, 161 tests green. **Why now, mid-pause:** history never backfills, so ~10× more games starting to accumulate today is worth ~10× the eventual data vs. adding this after the wait. **Open for the user: this work is uncommitted, and the next scheduled cron run will begin filling the larger corpus into Turso.**_
- _2026-07-17: **Task #37 complete — testing.** Added two pure-function suites, 149 tests, no DB/network/fixtures: **Vitest** (`npm test`, 73) over the earnings model (DevEx rate boundaries, band ordering, the always-`isEstimate` flag, div-by-zero guards), the validation layer (every rejection reason, dedupe, the keep-game-warn-on-missing-votes rule), and the stats/retention helpers (incl. the planner's idempotency + ISO-week year boundary, previously only checked by a throwaway script); and **pytest** (`npm run test:py`, 76) over the Python analytics — the locked dead rule, momentum, opportunity, survival's left-truncation guard, seasonality, and anomaly detection. Set up leaner than Next's Vitest guide on purpose (no jsdom/plugin-react — every page is an async Server Component, which that guide says to E2E-test; no `vite-tsconfig-paths` — Vitest 4 says it's redundant now). **The tests earned their keep immediately:** anomaly detection (#25) was flagging noise as "notable changes" — a z-score alone only asks "unusual for this series?", so on a regular curve MAD collapses and a 2% wiggle scores z=36; on real collected data **11 of 15 flags were noise** (a −3.4% drop; a +7.9% move at z=149). Fixed by also requiring material size (`MIN_ABS_CHANGE`), verified to drop all 11 while keeping a genuine +165% spike. Also fixed `npm run analytics`, **broken on Windows since Task #21** (cmd.exe can't resolve the forward-slashed venv path; CI never noticed because it calls python directly). New `.github/workflows/test.yml` gates every push/PR on both suites + typecheck + lint. **Next: Task #38** ("About the data" page) or #12._
